Worker identity chip: known agents get their species emoji on a sea-glass circle (🦀🪸🐚) — the one sanctioned emoji use, visual identity only; unknown names fall back to mono initials on a hashed accent circle; the human is 🧍 and reads "you".

```jsx
<AgentChip name="reef-crab" />
<AgentChip human size="sm" />
```
