Vertically scrollable content with a soft fade only at the clipped edge. At the top there is no top fade; at the bottom there is no bottom fade. Uses the standard `.tp-scroll` scrollbar vocabulary.

```jsx
<FadeScroll style={{ maxHeight: 320, overflowY: 'auto' }}>…</FadeScroll>
```
