Ink-dark toast for board-operation confirmations. States facts, never asks acks.

```jsx
<Toast kind="success" detail="tp-0144">moved to front of queue</Toast>
<Toast kind="warn">watchdog killed after 2h — question created</Toast>
```
