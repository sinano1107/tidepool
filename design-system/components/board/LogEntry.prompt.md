Decision-log line for the skim: time · agent · task id · one-line decision. No ack UI — hover reveals the single Object action; completions get a grass fill; unread entries a teal left bar.

```jsx
<LogEntry entry={{ time: '03:52', taskId: 'tp-0139', agent: 'anemone', kind: 'completion', text: 'criteria met — PR #58', unread: true }} onObject={open} />
```
