Decision-log line for the skim: time · agent · task id · one-line decision. Tapping a row opens the single Object action; a completion's right-edge chevron opens its handoff. Completions get a grass fill; unread entries a teal left bar.

```jsx
<LogEntry entry={{ time: '03:52', taskId: 'tp-0139', agent: 'anemone', kind: 'completion', text: 'criteria met — PR #58', unread: true }} onObject={open} />
```
